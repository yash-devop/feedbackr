/**
 * The `/features` folder contains feature-specific modules.
 *
 * Each feature is self-contained and can have its own
 * components, hooks, pages, services, and utilities.
 *
 * @example
 * /features/auth
 * /features/dashboard
 *
 * // Inside a feature folder:
 * /features/auth/components
 * /features/auth/hooks
 * /features/auth/pages
 */
